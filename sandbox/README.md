# sandbox

`sandbox/` 是科研脚本默认工作区。

- 相对路径工具默认解析到这里。
- `run_command` 默认在这里执行。
- 建议把实验脚本、参数扫描脚本、临时结果和分析原型都放在这里。

内置了两个示例：

- `hello_sim.py`: 一个很快结束的小仿真。
- `long_sim.py`: 一个持续运行 20 秒以上的脚本，用来验证硬件心跳与长任务监控。
